<?xml version="1.0" encoding="UTF-8"?>

<configuration scan="true">

    <property name="LOG_PATH" value="logs"/>

    <!-- ================================================= -->
    <!-- Console Pattern                                    -->
    <!-- ================================================= -->

    <property name="CONSOLE_PATTERN"
              value="%cyan(%d{yyyy-MM-dd HH:mm:ss.SSS}) %-5level [%thread] %green(%logger{36}) - %msg%n"/>

    <!-- ================================================= -->
    <!-- File Pattern                                       -->
    <!-- ================================================= -->

    <property name="FILE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{user}] [%X{requestId}] %logger - %msg%n"/>

    <!-- ================================================= -->
    <!-- Console                                            -->
    <!-- ================================================= -->

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>

            <pattern>${CONSOLE_PATTERN}</pattern>

        </encoder>

    </appender>

    <!-- ================================================= -->
    <!-- Application Log                                    -->
    <!-- ================================================= -->

    <appender name="APPLICATION"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_PATH}/application.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>

                ${LOG_PATH}/archive/application.%d{yyyy-MM-dd}.%i.log.gz

            </fileNamePattern>

            <maxFileSize>10MB</maxFileSize>

            <maxHistory>30</maxHistory>

            <totalSizeCap>2GB</totalSizeCap>

        </rollingPolicy>

        <encoder>

            <pattern>${FILE_PATTERN}</pattern>

        </encoder>

    </appender>

    <!-- ================================================= -->
    <!-- Error Log                                          -->
    <!-- ================================================= -->

    <appender name="ERROR"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_PATH}/error.log</file>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">

            <level>ERROR</level>

        </filter>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>

                ${LOG_PATH}/archive/error.%d{yyyy-MM-dd}.%i.log.gz

            </fileNamePattern>

            <maxFileSize>10MB</maxFileSize>

            <maxHistory>90</maxHistory>

        </rollingPolicy>

        <encoder>

            <pattern>${FILE_PATTERN}</pattern>

        </encoder>

    </appender>

    <!-- ================================================= -->
    <!-- Audit Log                                          -->
    <!-- ================================================= -->

    <appender name="AUDIT"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_PATH}/audit.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">

            <fileNamePattern>

                ${LOG_PATH}/archive/audit.%d{yyyy-MM-dd}.log.gz

            </fileNamePattern>

            <maxHistory>180</maxHistory>

        </rollingPolicy>

        <encoder>

            <pattern>${FILE_PATTERN}</pattern>

        </encoder>

    </appender>

    <!-- ================================================= -->
    <!-- Async Logging                                      -->
    <!-- ================================================= -->

    <appender name="ASYNC_APPLICATION"
              class="ch.qos.logback.classic.AsyncAppender">

        <queueSize>5000</queueSize>

        <discardingThreshold>0</discardingThreshold>

        <appender-ref ref="APPLICATION"/>

    </appender>

    <appender name="ASYNC_ERROR"
              class="ch.qos.logback.classic.AsyncAppender">

        <appender-ref ref="ERROR"/>

    </appender>

    <!-- ================================================= -->
    <!-- Audit Package                                      -->
    <!-- ================================================= -->

    <logger name="com.genai.ollamarestapi.audit"

            level="INFO"

            additivity="false">

        <appender-ref ref="AUDIT"/>

    </logger>

    <!-- ================================================= -->
    <!-- Application                                        -->
    <!-- ================================================= -->

    <root level="INFO">

        <appender-ref ref="CONSOLE"/>

        <appender-ref ref="ASYNC_APPLICATION"/>

        <appender-ref ref="ASYNC_ERROR"/>

    </root>

</configuration>